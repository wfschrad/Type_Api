const { port, socketPort } = require("../config");

const { app, http } = require("../server");
app
app.listen(port, () => console.log(`Listening on port ${port}...`));
http.listen(socketPort, () => console.log(`Socket server listening on port ${socketPort}`));
